<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Detalle de tu Producto</title>
	<link rel="stylesheet" href="/css/styles.css">
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
	<div class="container">
	<h2 class="alert alert-success mt-4"><%= product.name %> </h2>
	 <div>
	  <p>Nombre del producto: <%= product.name %></p>
	  <p>Precio: $<%= product.price %></p>
	  <p>Descripción: <%= product.description %> </p>
	  <p>Marca: <%= product.brand %></p>
	</div>
	<img src="<%= product.images.includes('http') ? product.images : '/images/' + product.images%>" width='200px'>
	
	<% if (admin === 'Grupo5') {%>
	<form action="/products/delete/<%= product._id %>" method="POST">
		<a class="btn btn-primary" type="button" href="/products/edit/<%= product._id %>"> Editar </a>
		<button class="btn btn-danger">Borrar</button>
	</form>
	<br>
	
	<% } %>
    </div>
	
</body>
           <%- include('./partials/footer'); -%>
</html>